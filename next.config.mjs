/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/Supreet.github.io',  // Replace 'repo-name' with your actual repository name
    images: {
      unoptimized: true,
    },
  };
  
  export default nextConfig;
  